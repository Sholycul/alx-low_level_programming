#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - prints a string in reverse.
 * @s: string
 * Return: length of the string
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
