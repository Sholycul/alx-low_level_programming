#include <stdio.h>
/**
 * main - it prints the function name
 *
 * @argc: integer number of arguments
 * @argv: Characters themselves
 *
 * Return: Always 0
 */

int main(int argc __attribute__((unused)), char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
